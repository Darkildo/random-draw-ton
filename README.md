🇷🇺 Кратко
Контракт случайных розыгрышей TON на Tolk. Создаёт розыгрыши, принимает ставки, выбирает победителя и выплачивает приз с учётом комиссии (fee).

🇺🇸 Brief
TON random draw contract (Tolk). Creates draws, accepts entries, picks a winner, pays prize minus fee.

## Текущая логика
- `CreateDraw(drawId, minEntryAmount, entryLimit, value)` — заводит розыгрыш с начальными средствами `value`, пустыми участниками и счётчиком `participantCounter = 0`.
- `LuckRoll(drawId)` — пополнение пула и запись участника. Если `value < minEntryAmount`, происходит рефанд (возврат остатка входящего сообщения). Если `poolSum >= entryLimit`, выбирается победитель по индексу `random.range(participantCounter)` в словаре участников (лексикографический обход ключей) и выплачивается `(poolSum * (100 - fee)) / 100`; комиссия сети удерживается из выплаты (send mode 0), после чего розыгрыш удаляется.
- `TopUpTons` или пустое тело — просто пополнение баланса контракта, на розыгрыши не влияет.
- `get_draw(drawId)` — возвращает структуру Draw с `participantCounter` и картой участников.

## Безопасность/ограничения
- Комиссии сети при выплате списываются из отправляемой суммы (контракт не доплачивает сверху).
- Рефанд при недоплате гарантирован (`value < minEntryAmount`).
- Псевдослучайность пока простая (`random.initialize(); random.range(...)`); улучшение источника энтропии требует доработки.

## Тесты
- Покрыты основные сценарии: создание, двойное создание, рефанд, minEntryAmount, суммирование ставок, уникальные участники, одиночный участник, распределение приза, удаление розыгрыша, пустое тело, неправильный opcode.
- Запуск: `npm test`

## Сборка и деплой
- Сборка контрактов: `npx blueprint build`
- Тесты: `npx blueprint test`
- Запуск скриптов: `npx blueprint run`
